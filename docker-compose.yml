# Stub Compose file for local service dependencies. The codexcw service builds
# the local image from this repo's Dockerfile.
services:
  codexcw:
    build:
      context: .
      target: codexcw
    image: codexcw:local
    # ports:
    #   - "8080:8080"
    # env_file:
    #   - .env
    # depends_on:
    #   - postgres

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: codexcw
  #     POSTGRES_PASSWORD: codexcw
  #     POSTGRES_DB: codexcw
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

# volumes:
#   postgres-data:
