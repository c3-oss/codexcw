# Builds per-platform native addons and publishes @c3-oss/codexcw to npm.
#
# Triggered by tags of the form `node-v*`. The build matrix produces the native
# addons; the publish job ships the per-platform packages and the main package
# with npm provenance via OIDC trusted publishing.
name: Release (npm)

on:
  push:
    tags: ["node-v*"]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

defaults:
  run:
    working-directory: bindings/node

jobs:
  build:
    name: build / ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-latest, target: aarch64-unknown-linux-gnu, napi_cross: true }
          - { os: ubuntu-latest, target: x86_64-unknown-linux-musl, zig: true }
          - { os: macos-latest, target: x86_64-apple-darwin }
          - { os: macos-latest, target: aarch64-apple-darwin }
          - { os: windows-latest, target: x86_64-pc-windows-msvc }
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24

      # Pin to the repo's rust-toolchain.toml channel so the cross target's
      # rust-std lands on the toolchain `napi build` actually uses; installing
      # it on `stable` leaves the pinned toolchain without `core` for the target.
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.90.0"
          targets: ${{ matrix.target }}

      # @napi-rs/cli >= 3.8 rejects --use-napi-cross for musl; cargo-zigbuild
      # via --cross-compile is the supported path and needs zig on PATH.
      - name: Setup Zig
        if: matrix.zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Install dependencies
        run: npm install

      - name: Build addon
        run: >-
          npx napi build --platform --release
          --target ${{ matrix.target }}
          ${{ matrix.napi_cross && '--use-napi-cross' || '' }}
          ${{ matrix.zig && '--cross-compile' || '' }}
          --js binding.js --dts binding.d.ts

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: bindings-${{ matrix.target }}
          path: bindings/node/*.node
          if-no-files-found: error

  publish:
    name: publish
    needs: build
    if: startsWith(github.ref, 'refs/tags/node-v')
    # Runs on a GitHub-hosted runner: npm provenance and the OIDC
    # trusted-publishing token exchange are both only available there.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm install

      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: bindings/node/artifacts

      - name: Distribute artifacts into npm dirs
        run: npx napi artifacts --npm-dir npm --build-output-dir artifacts

      # Trusted publishing needs npm >= 11.5.1; Node's bundled npm may be older.
      - name: Ensure npm supports trusted publishing
        run: npm install -g npm@latest

      - name: Publish
        run: |
          npx napi pre-publish -t npm --no-gh-release
          npm publish --access public --provenance --ignore-scripts
